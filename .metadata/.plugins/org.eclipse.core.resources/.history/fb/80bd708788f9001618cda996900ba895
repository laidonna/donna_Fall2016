import java.io.*;
import java.util.Scanner;

public class TempFileDiff {

	public static void main(String[] args) throws FileNotFoundException {
		Scanner console = new Scanner(new File("src/weather.txt"));		
		double change = 0.0;
		double first, second, next, third;
		while (console.hasNextDouble()){
			first = console.nextDouble(); 
			second = console.nextDouble();
			third = console.nextDouble();
			change = second - first;
			System.out.println(first + " to " + second + ", change = " + change);
			next = second; 
			change = second - third;
			System.out.println(next + " to " + third + ", change = " + change);
		}
	}
}
